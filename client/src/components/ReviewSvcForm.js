import React from 'react';

const ReviewSvcForm = () => {
    return (
        <div>
            I will be a form in which one might review a service
        </div>
    );
};

export default ReviewSvcForm;